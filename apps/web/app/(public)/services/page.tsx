import Link from 'next/link'; import { services } from '@/lib/data';
export default function Page() { return <div className="container-shell py-16"><h1 className="text-4xl font-bold">All Services / All Pujas</h1><div className="mt-10 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{services.map((service) => <Link key={service.slug} href={`/services/${service.slug}`} className="card p-6"><h2 className="text-2xl font-semibold">{service.titleEn}</h2><p className="mt-2 text-sm text-brand-vermilion">{service.titleHi}</p><p className="mt-3 text-sm text-stone-600">{service.shortDescription}</p><p className="mt-4 text-sm font-semibold text-amber-700">{service.price}</p></Link>)}</div></div>; }