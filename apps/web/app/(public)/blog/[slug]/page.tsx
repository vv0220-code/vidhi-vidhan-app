import { notFound } from 'next/navigation'; import { blogPosts, getBlogPost } from '@/lib/data';
export function generateStaticParams() { return blogPosts.map((post) => ({ slug: post.slug })); }
export default async function Page({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const post = getBlogPost(slug); if (!post) return notFound(); return <article className="container-shell py-16"><p className="text-sm font-semibold uppercase tracking-[0.3em] text-brand-vermilion">{post.category}</p><h1 className="mt-3 max-w-4xl text-5xl font-bold">{post.title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-stone-700">{post.excerpt} This launch-ready content architecture is designed for festival, muhurat, puja guidance, and local search intent while remaining easy for admin teams to expand later.</p></article>; }