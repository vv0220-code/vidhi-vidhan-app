import type { MetadataRoute } from 'next'; import { blogPosts, cities, services } from '@/lib/data';
export default function sitemap(): MetadataRoute.Sitemap { const base = 'https://vidhividhan.in'; return ['','/about','/contact','/services','/book-pandit','/faq','/blog'].map((path) => ({ url: `${base}${path}`, lastModified: new Date() })).concat(services.map((service) => ({ url: `${base}/services/${service.slug}`, lastModified: new Date() })), cities.map((city) => ({ url: `${base}/cities/${city.slug}`, lastModified: new Date() })), blogPosts.map((post) => ({ url: `${base}/blog/${post.slug}`, lastModified: new Date() }))); }